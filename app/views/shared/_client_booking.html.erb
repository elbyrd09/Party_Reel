<div class="event-card my-5">

  <div class="event-infos">
    <div>
      <h6><strong><%= booking.attendee.user.first_name %> <%= booking.attendee.user.last_name %></strong></h6>
      <h5> Package Selected: <%= booking.package.name %> </h5>
      <h2><%= booking.event.name %></h2>
      <p><%= booking.event.start_date %></p>
    </div>
    <h5 class="event-infos-pricing">Payment Status: Paid in full</h5>
    <img src="<%= asset_path("jd.jpg") %>" class="pgr-avatar-user avatar-bordered" /> <!-- attendee profile picture -->
  </div>
</div>
