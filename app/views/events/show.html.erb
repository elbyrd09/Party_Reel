<div class="container">
  <div class="row">
    <div class="col-12 col-lg-6">
      <h2>Book your Photographer for <%= @event.name %>! </h2>
      <p>Dates for <%= @event.name %>: </p>
      <p>Start date: <%= @event.start_date %> </p>
      <p>End date: <%= @event.end_date %> </p>

      <a href="<%= @event.id %>/packages">Book!</a>
    </div>
  </div>
</div>
